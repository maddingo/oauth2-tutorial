## PKCE React Client Application

The folder `spa-app` contains a sample React single page application (SPA) that demonstrates the OAuth2 Authorization Code Flow with PKCE.
You can run this client with Node.js
- `npm install`
- `npm run build`
- `npm run serve`
