management:
  httpexchanges:
    recording: true

logging:
  level:
    org.springframework.security: trace
    org.springframework.web: trace
spring:
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://idp:9000
debug: true
server:
  port: 8090
