server:
  port: 8080
logging:
  level:
    root: INFO
    org.springframework.web: TRACE
    org.springframework.security: TRACE
    no.lyse.plattform: TRACE
    sun.rmi: ERROR
    java.io: ERROR
    javax.management: ERROR
    reactor.netty: ERROR
    org.springframework.boot.availability: DEBUG
#    org.springframework.security.oauth2: INFO
#    org.springframework.boot.autoconfigure: DEBUG

spring:
  codec:
    log-request-details: true

  security:
    oauth2:
      client:
        registration:
          messaging-client-oidc:
            provider: spring
            client-id: messaging-client
#            client-authentication-method: none
            client-secret: 'secret'
            authorization-grant-type: authorization_code
            redirect-uri: "http://client-app:8080/oauth2/code/{registrationId}"
            scope: openid, profile, message.read,message.write
            client-name: messaging-client-oidc
#          messaging-client-authorization-code:
#            provider: spring
#            client-id: messaging-client
#            client-secret: secret
#            authorization-grant-type: authorization_code
#            redirect-uri: "http://localhost:8080/authorized"
#            scope: message.read,message.write
#            client-name: messaging-client-authorization-code
          messaging-client-client-credentials:
            provider: spring
            client-id: messaging-client
            client-secret: secret
            authorization-grant-type: client_credentials
            scope: message.read,message.write
            client-name: messaging-client-client-credentials
        provider:
          spring:
            issuer-uri: http://idp:9000
client-app:
  cors:
    allowed-origins: http://localhost:8080,http://localhost:3000,http://client-app:8080
    allowed-methods: GET, POST
  logout-redirect-uri: '/'
messages:
  base-uri: http://localhost:8090/

management:
  endpoint:
    health:
      probes:
        enabled: true
